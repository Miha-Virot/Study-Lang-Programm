#include <iostream>
#include <fstream>

using namespace std;

/*
int main(){
    ifstream in;  // Поток in будем использовать для чтения
    ofstream out; // Поток out будем использовать для записи
    in.open("input.txt");
    out.open("output.txt");
    out << x; // вывод значения
    in >> x; // ввод значения
    in.close(); // закрыть ранее открытый файл в потоке
    out.close(); // 
}
*/

int maint()
{
    ifstream in;
    ofstream out;
    in.open("input.txt");
    out.open("output.txt");
    int a, b = 0;
    string S;
}